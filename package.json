{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predev": "node ./scripts/prepare-md.js",
    "build": "vite build",
    "prebuild": "node ./scripts/prepare-md.js",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "chroma-js": "^3.1.2",
    "gh-pages": "^6.2.0",
    "gsap": "^3.12.5",
    "markdown-it": "^14.1.0",
    "vite": "^5.4.10"
  }
}
